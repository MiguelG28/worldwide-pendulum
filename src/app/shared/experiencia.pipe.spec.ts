import { ExperienciaPipe } from './experiencia.pipe';

describe('ExperienciaPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienciaPipe();
    expect(pipe).toBeTruthy();
  });
});
